import 'package:flutter/material.dart';

SizedBox bitcoinLogoBuilder() {
  return SizedBox(
      height: 80,
      width: 80,
      child: Image.asset('assets/images/bitcoin_image.png'));
      //Image is not a perfect one. So might not be looks good in darkmode.
}
